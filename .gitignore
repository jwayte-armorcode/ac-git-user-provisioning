# Real credentials — never commit. Use env.example as the template.
envfile
env_gitlab
env_github
env
*.env

# Runtime state — regenerated per run, not source. Both are per-source by
# default (sync_checkpoint_github.json, email_exceptions_gitlab.csv, ...);
# the bare names are the pre-split defaults and any --checkpoint-file /
# --exceptions-file override.
sync_checkpoint*.json
*-repos.csv
*-repos-changed.csv
apply_cache_*.json
email_exceptions*.csv
unmatched_repos*.csv
repos.json
users.json
teams.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
