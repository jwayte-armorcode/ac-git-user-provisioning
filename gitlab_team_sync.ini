[armorcode]
# tenantRole assigned to newly-created ArmorCode users provisioned from
# GitLab project members. Must be a valid role name in the tenant — check
# GET /user/roles (e.g. Developer, Admin, DevOps, Security Engineer, Read Only,
# or a Custom_* role). Wrong values return 400 "Provided Tenant Role Not Found".
default_role = Developer
